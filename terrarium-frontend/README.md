# 🦎 Terrarium Frontend

Панель управления климатом террариума — Angular 19 + TailwindCSS 4 + Apache ECharts.

## Требования

- **Node.js** ≥ 20
- **npm** ≥ 10
- Работающий бэкенд `terrarium-core` на `http://localhost:8080`

## Запуск (разработка)

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера (http://localhost:4200)
npm start
```

> Все API-запросы идут напрямую на `http://localhost:8080/api/v1` (настроено в `src/environments/environment.ts`).

## Сборка (production)

```bash
npm run build
```

Результат сборки — в `dist/terrarium-app/browser/`. В Docker собирается и отдаётся через nginx с проксированием API.

## Настройка API URL

- **Разработка** — файл `src/environments/environment.ts` → `apiUrl`
- **Production** — файл `src/environments/environment.prod.ts` → `apiUrl: '/api/v1'` (nginx прокси)
- **Polling-интервал** — `pollingIntervalMs` (по умолчанию 5000 мс)

## Структура проекта

```
src/app/
├── core/               # Сервисы и модели
│   ├── models/         # TypeScript-интерфейсы (API типы)
│   └── services/       # ApiService, PollingService, ToastService
├── pages/              # Lazy-loaded страницы
│   ├── dashboard/      # Главная: показания датчиков, статус реле
│   ├── relays/         # Ручное управление реле (ON/OFF)
│   ├── automation/     # Конфигурация порогов, расписания
│   ├── history/        # Графики ECharts (температура, влажность, энергия)
│   └── system/         # Статус системы, журнал переключений
└── shared/             # Переиспользуемые компоненты
    └── components/     # Sidebar, Toast и т.д.
```

## Стек технологий

| Технология | Назначение |
|------------|-----------|
| Angular 19 | Фреймворк (standalone, lazy-loading, signals) |
| TailwindCSS 4 | Стили (кастомная cyberpunk-тема) |
| Apache ECharts | Интерактивные графики (zoom, tooltips, datazoom) |
| RxJS | Polling, HTTP-запросы, реактивные потоки |

## Docker

Dockerfile уже настроен — multi-stage сборка (node → nginx).  
Запуск через `docker-compose up` из корня репозитория.
